enum Preferenza {
  MASCHIO
  FEMMINA
  ENTRAMBI
}

enum Sesso {
  MASCHIO
  FEMMINA
}

type Users @model @auth(rules: [{allow: public}]) {
  id: ID!
  Nome: String
  Cognome: String
  Email: String
  DataNascita: AWSDate
  Sesso: Sesso
  Preferenza: Preferenza
  Descrizione: String
}
